<div class="row resa-info">
    {% for label, flashes in app.session.flashbag.all %}
        <div class="bg-info text-info text-center  col-sm-4 col-sm-offset-4 col-xs-8 col-xs-offset-2">
            {% for flash in flashes %}
                <p> {{ flash }} </p>
            {% endfor %}
        </div>
    {% endfor %}
</div>